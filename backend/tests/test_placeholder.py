"""Placeholder test for M0.

Confirms the package imports and the test runner is wired up. Real tests for
the data model, attribution, and reconciliation logic arrive in M1+.
"""

import meter


def test_package_has_version():
    assert meter.__version__ == "0.1.0"
